/* Desenvolver um programa que apresente todos os valores numéricos inteiros ímpares situados na faixa de 1000
a 1500. */

/* let cont = 1000;
let impar = cont % 2 !== 0;

for(cont; cont <= 1500; cont++){
    if(cont === impar){
        console.log(cont);
    }
}
    O CÓDIGO COMO COMENTÁRIO É UM O CÓDIGO QUE ESTAVA TENTANDO CRIAR, JA O DE BAIXOU, PEGUEI DA IA, 
*/ 

for (let cont = 1000; cont <= 1500; cont++) {
    if (cont % 2 !== 0) {
        console.log(cont);
    }
}